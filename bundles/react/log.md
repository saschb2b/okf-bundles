## 2026-07-24

- **Enrichment (react-compiler Rust port)**: Added `rust-port` concept covering the React team's official Rust reimplementation (Joseph Savona, `facebook/react` PR #36173, merged 2026-06-09, experimental): motivation (build speed, native swc/oxc/Rspack integration), architecture parity (same HIR/CFG/SSA and passes, arena allocation, 1725 fixtures byte-for-byte), attributed performance numbers (~3x as Babel plugin, up to 10x transform logic, Turbopack 40%+, Next.js 16.3 experimental), toolchain integration (oxc vendored crates, swc migrated, Rspack 2.1, oxlint rule, Rolldown withdrawn), and caveats (unpublished, LLM-assisted, non-idiomatic Rust concerns). Corrected `roadmap`'s "non-Babel backends" section, which had framed the backend story as merely invoking the TS compiler; added a 2026 milestone row to `adoption-timeline`; wired into the section index and `how-it-works`. Distinguished the official port from prior community experiments. Grounded in the PR and InfoQ; numbers attributed as early/directional.
- **Enrichment (react-compiler glossary split)**: For digestibility, gave three load-bearing internal terms their own concept homes under a new `glossary/` folder: `hir` (the CFG representation, instructions, `Place`, and the worked HIR and SSA/phi examples), `mutability-model` (effect kinds, aliasing, mutable ranges, freeze point), and `reactive-scope` (the memoization unit, dependencies, mapping to cache slots). Trimmed `how-it-works` and `memoization-model` to link to these homes instead of re-explaining, so the pipeline concept reads as a clean pass-by-pass flow. Added `glossary/index.md`, wired the glossary into the section index and both deep-dive concepts' Related sections. Content relocated, not lost; strict validator clean (0 orphans, 0 broken links).
- **Enrichment (react-compiler internals)**: Via `/okf enrich`, deepened `how-it-works` and `memoization-model` with real under-the-hood detail: the actual `Pipeline.ts` pass names in execution order (`lower`, `dropManualMemoization`, `enterSSA`/`eliminateRedundantPhi`, `inferTypes`, the `validate*` family, `inferMutationAliasingEffects`/`Ranges`, `inferReactivePlaces`, `inferReactiveScopeVariables`, `alignReactiveScopesToBlockScopesHIR`, `mergeOverlappingReactiveScopesHIR`, `propagateScopeDependenciesHIR`, the `prune*` scope-optimization passes, `validatePreservedManualMemoization`, `codegenFunction`), a worked HIR/CFG lowering example, an SSA-with-phi example, a Mermaid pipeline diagram, and a full multi-slot before/after codegen. Mapped validation passes to their ESLint-lint counterparts. Grounded in the compiler source `Pipeline.ts`; no invented pass names.
- **Enrichment (react-compiler)**: Deepened the learn-side React Compiler section from 6 to 13 concepts for a holistic deep dive. Added `how-it-works` (the build-time pipeline: HIR lowering to a CFG, SSA, type and mutability/aliasing inference, reactive scope inference, codegen) and `memoization-model` (reactive scopes, the per-component `_c` cache and slots, the `react.memo_cache_sentinel`, why the output beats manual memo and why re-render cascades stop). Added `adoption-timeline` (Conf 2024, Oct 2024 beta, Apr 2025 RC, Oct 7 2025 1.0, late-2025 ecosystem reckoning), `production-experience` (boring wins and the silent opt-out failure mode with detection), `linting` (eslint-plugin-react-hooks v6+ merge, compiler-relevant rules), `migration-playbook` (incremental sequence, the one-big-PR trap, per-framework how-tos), and `roadmap` (finer control, compiler-aware RSC, DevTools gap, React Native, swc/oxc; framed as directional). Grounded technical claims in the compiler design docs and the 1.0 release notes; ingested and cited saschb2b.com's year-in-review for the ecosystem arc. Wired all new concepts into the section index, the landing concept, and inbound links from `introduction` and `debugging`; refreshed those two timestamps. Bundle stays validator-clean and graph-clean.

## 2026-06-30

- **Creation**: Exported the official React documentation (react.dev) into a conformant OKF bundle: 161 concepts across the Learn guide (get-started, installation, setup, React Compiler, and the four core sections: Describing the UI, Adding Interactivity, Managing State, Escape Hatches) and the full API Reference (react Hooks/components/APIs/legacy, react-dom components/hooks/APIs/client/server/static, React Compiler config and directives, Rules of React, the eslint-plugin-react-hooks lints, React Server Components, and React DevTools). One concept per documentation page, each with its canonical react.dev URL in `resource`, transformed into structural markdown rather than copied. Built by fan-out over react.dev `.md` pages, with cross-links wired to the bundle folder map.
- **Update (graph)**: Made the bundle graph-clean for the OKF visualizer. Repointed concept links from folder `index.md` listings to concepts, dropped an in-page anchor, and wired 8 orphan concepts (`act`, `captureOwnerStack`, `useDebugValue`, and the `immutability`/`refs`/`set-state-in-render`/`unsupported-syntax`/`use-memo` lints) with `# Related` cross-links. Result: 162 concepts, 606 edges, 0 concept-to-index links, 0 orphans. Content unchanged.
